compressionLevel: mixed

enableGlobalCache: false

nodeLinker: node-modules

supportedArchitectures:
  cpu:
    - current
    - arm64
  libc:
    - current
    - glibc
    - musl
  os:
    - linux
    - current
