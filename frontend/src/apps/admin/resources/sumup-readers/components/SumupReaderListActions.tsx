import React from "react";
import { TopToolbar, CreateButton } from "react-admin";

export const SumupReaderListActions: React.FC = () => (
  <TopToolbar>
    <CreateButton label="Pair" />
  </TopToolbar>
);

export default SumupReaderListActions;
